[package]
name = "photo-organizer"
version = "0.1.0"
edition = "2021"
description = "A CLI tool to organize photos by EXIF capture date"

[dependencies]
clap = { version = "4", features = ["derive"] }
kamadak-exif = "0.5"
chrono = "0.4"
walkdir = "2"
anyhow = "1"
